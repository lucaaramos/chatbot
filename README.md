# GPT-2 Conversational Chatbot

Este es un chatbot simple en Python que utiliza el modelo **GPT-2** de [Hugging Face Transformers](https://huggingface.co/) para generar respuestas a partir de la entrada del usuario.  

## 📋 Requisitos

Antes de ejecutar el chatbot, asegúrate de tener instaladas las dependencias necesarias:

```bash
pip install transformers torch
# chatbot
